# Coding Style Preferences

The project follows PEP 8 for Python code with type hints throughout. Docstrings use the Google style format. All modules expose a consistent interface with `async` methods. Error handling uses structured exceptions rather than bare try/except blocks.
